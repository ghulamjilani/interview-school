<div id='new-teacher-subject'>
  <% if @teacher.subjects.size < Subject.all.size %>
    <%= link_to 'Add new subject', new_teacher_teacher_subject_path(@teacher), remote: true %>
  <% else %>
    Please <%= link_to 'create new subjects', new_subject_path %> so you can add to Teacher
  <% end %>
</div>
